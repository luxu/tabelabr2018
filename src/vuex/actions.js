// import Vue from 'vue'

export default {
  // 'load-times' (context) {
  //   Vue.http.get('http://localhost:8080/dist/times.json')
  //     .then(response => {
  //       let times = response.data.map(element =>
  //         new Time(element.id, element.nome, element.escudo))
  //       context.commit('set-times', times)
  //     })
  // }
}
